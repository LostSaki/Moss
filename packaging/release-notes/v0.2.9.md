## Moss 0.2.9 (Pre-release)

**Beta / pre-release.** Use Settings → Updates → **Beta** to receive this build. Stable remains on the last full release (v0.2.2) until you promote later.

### Fixed

- GitHub update checks: proper API Accept headers, **certifi** CA bundle for frozen AppImage/portable SSL
- Clearer errors when the network/SSL fails (no more opaque “try again later” only)
- Stable channel: if only prereleases exist, Moss says so and points you to **Beta** instead of pretending GitHub is down

### Includes from 0.2.8

- Games database auto-install, update channels/rollback, support packs, launch UX

### Note

There is **no 0.3.0** product release. AI advisor remains experimental / future 0.3.x.
